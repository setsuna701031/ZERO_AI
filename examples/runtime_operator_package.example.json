{
  "package_id": "runtime-operator-package-example",
  "task_id": "runtime-operator-task-example",
  "goal": "Run the closed autonomous runtime loop through the operator console",
  "requested_mode": "controlled",
  "authority_context": {
    "operator": "RuntimeOperatorService",
    "approval_required": true,
    "executor_required": true,
    "governed_mutation_adapter_required": true,
    "validation_required": true,
    "rollback_required": true
  },
  "requested_changes": [
    {
      "change_id": "example-change-1",
      "path": "core/runtime/example_target.py",
      "operation": "governed_repo_edit",
      "description": "Example governed mutation request routed through sandbox and rollback boundaries"
    }
  ]
}
