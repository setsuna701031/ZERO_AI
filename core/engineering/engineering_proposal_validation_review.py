from __future__ import annotations
from typing import Any,Mapping
from core.engineering.engineering_proposal_review_common import ValidationResult,review_artifact,validate_review_artifact
SCHEMA="zero.engineering.proposal_validation_review.v1";ID_KEY="proposal_validation_review_id";PREFIX="engineering-proposal-validation-review-";FIELDS={"review_intake_id","proposal_validation_plan_linkage","coverage_checks","category_checks","criteria_checks","pre_approval_checks","pre_authorization_checks","post_implementation_checks","long_running_checks","environment_assumption_checks","validation_gap_findings"}
def build_engineering_proposal_validation_review(intake:Mapping[str,Any],closure:Mapping[str,Any],intent:Mapping[str,Any]|None=None)->dict[str,Any]:
 i=dict(intent or {}); gaps=sorted(set(i.get("validation_gaps",[]))); opaque=bool(i.get("require_detailed_validation") and not i.get("validation_evidence")); status="insufficient_evidence" if opaque else ("changes_required" if gaps else "sufficient");p={"review_intake_id":intake.get("proposal_review_intake_id"),"proposal_validation_plan_linkage":closure.get("engineering_proposal_id"),"coverage_checks":{"covered":not gaps},"category_checks":{"present":not gaps},"criteria_checks":{"defined":not gaps},"pre_approval_checks":"reviewed","pre_authorization_checks":"not_evaluated","post_implementation_checks":"preserved","long_running_checks":sorted(set(i.get("long_running_validations",[]))),"environment_assumption_checks":sorted(set(i.get("environment_assumptions",[]))),"validation_gap_findings":gaps};return review_artifact(SCHEMA,status,p,ID_KEY,PREFIX)
def validate_engineering_proposal_validation_review(v:Any)->ValidationResult:return validate_review_artifact(v,schema=SCHEMA,statuses={"sufficient","changes_required","blocked","invalid","insufficient_evidence"},id_key=ID_KEY,prefix=PREFIX,fields=FIELDS)
__all__=["build_engineering_proposal_validation_review","validate_engineering_proposal_validation_review"]
